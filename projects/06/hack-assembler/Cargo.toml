[package]
name = "hack-assembler"
version = "1.2.0"
edition = "2024"
rust-version = "1.92"

[dependencies]
thiserror = "2.0"
phf = { version = "0.11", features = ["macros"] }

[dev-dependencies]
proptest = "1.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
